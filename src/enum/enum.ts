export enum Role {
    USER = "USER",
    ADMIN = "ADMIN"
}

export enum AuctionStatus {
  WAITING_APPROVED = "WAITING_APPROVED",
  IN_PROGRESS = "IN_PROGRESS",
  CANCELED = "CANCELED",
  END = "END",
}